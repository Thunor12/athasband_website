---
interface Props {
  variant?: "default" | "feature";
  class?: string;
}

const { variant = "default", class: className } = Astro.props;
---

<div class:list={["panel", { feature: variant === "feature" }, className]}>
  <slot />
</div>

<style>
  .panel {
    border: 1px solid rgba(181, 150, 90, 0.24);
    background: rgba(0, 0, 0, 0.3);
    border-radius: 4px;
    padding: 0.8rem 0.95rem;
  }

  .panel.feature {
    border-color: rgba(181, 150, 90, 0.28);
    border-radius: 5px;
    background: linear-gradient(
      180deg,
      rgba(181, 150, 90, 0.1) 0%,
      rgba(0, 0, 0, 0.34) 100%
    );
    box-shadow: 0 0 18px rgba(181, 150, 90, 0.08);
    transition: box-shadow 280ms ease, border-color 280ms ease;
  }

  .panel.feature:hover {
    border-color: rgba(220, 196, 123, 0.4);
    box-shadow: 0 0 28px rgba(181, 150, 90, 0.16);
  }

  @media (prefers-reduced-motion: reduce) {
    .panel.feature {
      transition: none;
    }
  }
</style>
