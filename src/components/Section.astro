---
interface Props {
  title: string;
  sectionId?: string;
}

const { title, sectionId } = Astro.props;
---

<section class="section" id={sectionId}>
  <h2>{title}</h2>
  <div class="section-content">
    <slot />
  </div>
</section>
